##############################################
# Example .env file for Video Sharing App    #
##############################################

# Node & Application Environment

# Set the runtime environment: 'development', 'production', or 'test'
NODE_ENV=development

# Set the port for the Express server to listen on
PORT=5000

# Security & CORS

# Allowed origin(s) for CORS. Set to '*' for all, or a comma-separated list of origins. Example:
# CORS_ORIGIN=https://myfrontend.com,http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# Max file upload size in bytes (e.g., 104857600 for 100MB). Optional; defaults to code value if not set.
# MAX_FILE_SIZE=104857600

# AWS S3 configuration

# Your AWS access credentials
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# S3 bucket name (all are supported, first one present is used)
AWS_S3_BUCKET_NAME=your_bucket_name
# Alternative supported bucket name variables:
# AWS_BUCKET_NAME=your_bucket_name
# S3_BUCKET_NAME=your_bucket_name

# AWS region for the S3 bucket
AWS_REGION=us-east-1

# (Optional) Custom base public URL for your S3 bucket.
# If not set, will be constructed using bucket and region vars.
# Example: AWS_S3_PUBLIC_URL=https://your-bucket-name.s3.us-east-1.amazonaws.com
# AWS_S3_PUBLIC_URL=

# MongoDB configuration

# Connection URI for MongoDB (required)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/yourdb?retryWrites=true&w=majority

# (Optional) Explicit database name; overrides DB in URI if set
# MONGODB_DBNAME=yourdb


